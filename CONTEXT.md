# Agent Capability Observatory

观察 agent 系统完成客观可评分任务的表现，优先追踪固定配置随时间的变化，同时支持不同配置之间的比较。

## Language

**Harness**：承载被评测会话及其工具使用的 agent CLI，例如 Codex、OpenCode、Pi；用户指定其模型配置，可手动打开，也可由命令启动。

**被评测会话（Evaluated Session）**：在预先准备的受控容器中，以用户指定的 harness 和模型配置启动的全新会话；每个会话只通过 ACO API 领取、作答和提交一道题，不要求人工交互输入任务。
_Avoid_：仅代用户启动另一个会话的评测客户端

**管理面（Management Surface）**：供运行者登记版本、创建和控制评测批次、发起评分及查看结果的可信接口；被评测会话无权访问。

**会话面（Session Surface）**：仅供某个被评测会话领取其绑定题目、请求结束作答和查询交卷状态的最小权限接口；访问能力绑定单个计划样本，并在该样本结束时失效。

**技能配置（Skill Configuration）**：一次评测明确选择加载的 skills，默认不加载；不同技能配置可以用于比较同一 harness 和模型的作答表现。

**公有题（Public Task）**：题目资产公开发布、可供外部检查和复现的题目；公开属性描述可见性，不表示题目来自第三方。

**私有题（Private Task）**：题目资产不公开发布、只在获授权的评测流程中向被评测会话提供必要部分的题目，用于降低训练污染和答案泄漏风险；私有属性描述可见性，不表示题目一定由 ACO 创作。

**自构题（Authored Task）**：由 ACO 维护者原创或通过有来源记录的人工 mutation 构造的题目；可以是公有题或私有题。

**请求配置（Requested Configuration）**：运行者选择并交给启动端执行的 harness、provider、模型及相关评测条件，表示本次希望运行什么。

**凭证引用（Credential Reference）**：请求配置对某项运行凭证的逻辑引用；凭证值由可信启动端在本地解析，只向对应的被评测会话注入所需值，不进入 Registry、Trial 记录或产物。

**运行观测（Runtime Observation）**：启动与作答期间实际采集到的版本、配置、会话或服务响应等证据；每项有来源，缺失保持未知，不等同于供应商内部模型身份的证明。

**人工辅助作答（Human-assisted Run）**：人在作答期间提供解题提示、代改文件或提醒修正答案的作答；请求配置将其与独立作答分开记录和比较，必要的工具权限确认本身不属于解题辅助。

**能力表现退化（Performance Regression）**：固定 agent 配置在可比较任务上的完成表现随时间下降；该观测本身不确定下降来自模型、harness 或服务端行为。
_Avoid_：已证实的模型降智、已证实的模型路由切换

**封存答案（Sealed Answer）**：一次作答终止修改后保留、供正式评分使用的固定产物；交付重试和重新评分引用同一份答案，后续诊断采集不替换它。

**作答终止请求（Finish Request）**：被评测会话请求立即停止作答并封存当前工作区的幂等意图；请求本身不携带答案内容，Artifact Contract 声明范围内的封存文件是唯一正式答案来源。

**有效评分覆盖率（Valid Scoring Coverage）**：预先确定的计划样本中，满足执行条件并取得有效客观评分的比例；与有效样本表现一起展示，反映结果缺失程度。

**评测批次（Experiment）**：运行前确定题目版本、被测配置和重复样本清单的一组评测；历史曲线以批次为观测点。

**计划样本（Trial）**：评测批次内某题目、配置和重复编号对应的一次作答机会；准备失败仍保留，重新作答创建关联的新样本，重传答案和重评不创建新样本。

**评分记录（Verification Result）**：某一版本评分器对固定封存答案的一次评价记录；保留失败和重评历史，不覆盖原有记录。

**评分不稳定（Scorer Instability）**：同一版本评分器对同一封存答案产生相互矛盾的有效结果；相关结果保留但不进入正式能力统计，修复后的评分器必须使用新版本。
